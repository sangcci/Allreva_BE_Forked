<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- from observability.yml -->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="allreva"/>
    <springProperty scope="context" name="LOKI_URL" source="loki.url" defaultValue=""/>

    <!-- ==================== Console Appender ==================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{0}) [%X{correlationId}] [%X{user_id}] %X{method} %X{uri} %X{query_string} %X{status} %X{duration_ms}ms ip=%X{client_ip} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ==================== File Appender (local, dev) ==================== -->
    <springProfile name="dev">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>logs/application.log</file>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{0} [%X{correlationId}] [%X{user_id}] %X{method} %X{uri} %X{query_string} %X{status} %X{duration_ms}ms ip=%X{client_ip} - %msg%n</pattern>
                <charset>UTF-8</charset>
            </encoder>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>logs/application-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
                <maxFileSize>10MB</maxFileSize>
                <maxHistory>30</maxHistory>
            </rollingPolicy>
        </appender>
    </springProfile>

    <!-- ==================== Loki Appender (stag, prod) ==================== -->
    <springProfile name="stag,prod">
        <appender name="LOKI" class="com.github.loki4j.logback.Loki4jAppender">
            <http>
                <url>${LOKI_URL}</url>
            </http>
            <format>
                <label>
                    <pattern>app=${APP_NAME},env=${spring.profiles.active:dev},level=%level</pattern>
                </label>
                <message>
                    <pattern>
                        {
                          "timestamp": "%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}",
                          "level": "%level",
                          "thread": "%thread",
                          "logger": "%logger{36}",
                          "correlationId": "%X{correlationId:-}",
                          "user_id": "%X{user_id:-}",
                          "method": "%X{method:-}",
                          "uri": "%X{uri:-}",
                          "query_string": "%X{query_string:-}",
                          "status": "%X{status:-}",
                          "duration_ms": "%X{duration_ms:-}",
                          "client_ip": "%X{client_ip:-}",
                          "message": "%message",
                          "exception": "%ex{10}"
                        }
                    </pattern>
                </message>
                <sortByTime>true</sortByTime>
            </format>
        </appender>

        <appender name="ASYNC_LOKI" class="ch.qos.logback.classic.AsyncAppender">
            <appender-ref ref="LOKI"/>
            <queueSize>512</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
        </appender>
    </springProfile>

    <!-- ==================== Root Logger / Application Logger ==================== -->
    <springProfile name="test,local">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.backend.allreva" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE"/>
        </logger>
    </springProfile>

    <springProfile name="dev">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        <logger name="com.backend.allreva" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </logger>
    </springProfile>

    <springProfile name="stag,prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_LOKI"/>
        </root>
        <logger name="com.backend.allreva" level="INFO" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_LOKI"/>
        </logger>
    </springProfile>
</configuration>


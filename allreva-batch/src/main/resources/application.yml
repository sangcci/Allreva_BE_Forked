spring:
  application:
    name: allreva-batch
  config:
    import:
      - db.yml
      - kopis-client.yml
      - global-cache.yml
      - observability.yml
  main:
    web-application-type: none
  task:
    scheduling:
      thread-name-prefix: batch-scheduler-
